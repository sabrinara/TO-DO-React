import React from 'react';
import { TodoNoState } from './TodoNoState';

const noState = () => {
    return (
        <div className='flex justify-center items-center h-screen mx-auto'>
            <TodoNoState/>
        </div>
    );
};

export default noState;